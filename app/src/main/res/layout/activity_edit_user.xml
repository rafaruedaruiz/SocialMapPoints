<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#efeeee">

    <Button
        android:id="@+id/saveUserButton"
        android:layout_width="199dp"
        android:layout_height="54dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="30dp"
        android:layout_marginEnd="32dp"
        android:background="@drawable/button_rounded"
        android:elevation="8dp"
        android:text="Guardar cambios"
        android:textColor="#FFFFFF"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/usernameEditText" />

    <soup.neumorphism.NeumorphImageButton
        android:id="@+id/uploadProfilePhotoButton"
        style="@style/Widget.Neumorph.ImageButton"
        android:layout_width="88dp"
        android:layout_height="88dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginStart="8dp"
        android:layout_marginTop="50dp"
        android:contentDescription="@string/app_name"
        android:scaleType="centerInside"
        android:src="@drawable/change_photo_icon"
        android:visibility="visible"
        app:layout_constraintStart_toEndOf="@+id/editUserProfilePhoto"
        app:layout_constraintTop_toTopOf="@+id/editUserProfilePhoto" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/editUserProfilePhoto"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:layout_marginTop="32dp"
        app:civ_border_color="@color/white"
        app:civ_border_width="2dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/default_user_photo" />

    <EditText
        android:id="@+id/usernameEditText"
        android:layout_width="221dp"
        android:layout_height="68dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="25dp"
        android:layout_marginEnd="32dp"
        android:backgroundTint="#818181"
        android:ems="10"
        android:inputType="textPersonName"
        android:text="Username"
        android:textColor="#818181"
        android:textColorHint="#818181"
        android:textSize="36sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/editUserProfilePhoto" />

</androidx.constraintlayout.widget.ConstraintLayout>